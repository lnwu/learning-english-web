# Encrypted secrets file using SOPS
# This file is encrypted with Google Cloud KMS and can be safely committed to git
# Only those with KMS decrypt permissions can read the actual values

# Application Secrets (encrypted in repository)
auth:
  secret: CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32
  google:
    client_id: CHANGE_ME_YOUR_GOOGLE_OAUTH_CLIENT_ID
    client_secret: CHANGE_ME_YOUR_GOOGLE_OAUTH_CLIENT_SECRET

firebase:
  api_key: CHANGE_ME_YOUR_FIREBASE_API_KEY
  auth_domain: CHANGE_ME_YOUR_PROJECT.firebaseapp.com
  project_id: CHANGE_ME_YOUR_FIREBASE_PROJECT_ID
  storage_bucket: CHANGE_ME_YOUR_PROJECT.firebasestorage.app
  messaging_sender_id: CHANGE_ME_YOUR_MESSAGING_SENDER_ID
  app_id: CHANGE_ME_YOUR_FIREBASE_APP_ID
  measurement_id: CHANGE_ME_YOUR_MEASUREMENT_ID

# Instructions to encrypt this file:
# 1. Install SOPS: https://github.com/mozilla/sops
# 2. Run: sops --encrypt --gcp-kms projects/YOUR_PROJECT_ID/locations/global/keyRings/sops/cryptoKeys/sops-key secrets.yaml > secrets.enc.yaml
# 3. Delete this unencrypted file
# 4. Commit secrets.enc.yaml to git
