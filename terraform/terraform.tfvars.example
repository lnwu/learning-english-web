# Example Terraform variables file
# Copy this to terraform.tfvars and fill in your actual values
# NEVER commit terraform.tfvars with real credentials to git

# Google Cloud Configuration
gcp_project_id = "learning-english-477407"  # Your GCP project ID
gcp_region     = "us-central1"

# GitHub Configuration
github_repo = "lnwu/learning-english"

# Vercel Configuration
# Get these from: https://vercel.com/account/tokens
vercel_token             = "your-vercel-api-token"
vercel_org_id            = "your-vercel-org-id"
vercel_project_id        = "your-vercel-project-id"
vercel_project_name      = "learning-english"
vercel_production_domain = "learning-english-nine.vercel.app"  # Your actual Vercel domain

# ============================================================================
# That's ALL you need! Application secrets are AUTO-GENERATED by Terraform:
# ============================================================================
# 
# Terraform will automatically create and configure:
# ✅ OAuth Client ID and Secret (via Identity Platform)
# ✅ Firebase configuration (API key, auth domain, project ID, etc.)
# ✅ AUTH_SECRET (randomly generated)
# ✅ All 10 environment variables set in Vercel automatically
# 
# NO MANUAL SECRETS NEEDED!
# 
# After `terraform apply`:
# 1. All secrets are generated
# 2. All Vercel environment variables are set
# 3. Just add OAuth redirect URIs in GCP Console (one-time setup)
# 4. Deploy your app - it works!
# 
# Benefits:
# - Zero manual secret management
# - No encrypted files needed
# - Secrets managed in Terraform state (keep it secure)
# - Can recreate everything from scratch
# - Only 5 values to configure (all provider credentials)
