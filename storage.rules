rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default: deny all access
    // Customize based on your storage needs
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
