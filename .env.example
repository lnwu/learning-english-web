# Database Configuration
# For development, you can use:
# - Local PostgreSQL: postgresql://USER:PASSWORD@localhost:5432/learning_english
# - Prisma Postgres (local): Use the prisma dev command
# For production, use your cloud provider's connection string (e.g., Vercel Postgres, Supabase, Railway, etc.)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/learning_english"

# NextAuth.js Configuration
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here-replace-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
